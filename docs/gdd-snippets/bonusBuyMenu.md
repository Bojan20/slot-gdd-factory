# bonusBuyMenu — GDD snippet

> Wave LEGO-BUY (4 / 8) — Multi-tier bonus buy menu. @module bonusBuyMenu Purpose: When GDD declares `bonus_buy_menu` (or `bonus_buy.tiers[]`), this block replaces the single-cost `bonusBuy` button with a dropdown / sheet that exposes several buy tiers (e.g. Standard FS @ 75×, Super FS @ 200×, Mega FS @ 500×).

```yaml
bonusBuyMenu:
    enabled: false
    label: BUY BONUS
    tiers:
      - 
              id: standard
              label: STANDARD FS
              costX: 75
              forceScatters: 4
              fsMode: standard
              color: "#ff5050"
              colorDark: "#b03030"
    rearmMs: 1200
    confirmMessage: ""
    triggerColor: "#ff5050"
    triggerColorDark: "#b03030"
    triggerShadowRGB: "255,80,80"
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
