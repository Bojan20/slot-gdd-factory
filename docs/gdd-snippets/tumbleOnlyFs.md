# tumbleOnlyFs — GDD snippet

> Wave LEGO-FSV.3 — Tumble-only Free Spins (no fresh re-spin). Purpose ─────── FS variant where each "spin" is actually a tumble chain — no fresh reel spin, just cascade reductions until the board empties or the tumble chain dies.

```yaml
tumbleOnlyFs:
    enabled: false
    chainsBudget: 10
    hudBadgeColor: "#a8e6ff"
    hudPosition: topRight
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
