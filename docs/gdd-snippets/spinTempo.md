# spinTempo — GDD snippet

> Slot GDD Factory · spinTempo BLOCK Reel-spin cadence config — drives the windup → accel → steady → decel → stagger → cushion-bounce timing of every uniform-reel grid. Single SPIN_PROFILE object consumed by the reel engine in both BASE and FS (Boki rule: identical tempo across phases).

```yaml
spinTempo:
    windupMs: 100
    windupFrames: 7
    windupPx: 42
    accelMs: 130
    steadyMs: 720
    decelMs: 300
    staggerMs: 180
    bouncePx: 6
    bounceDecay: 0.3
    bounceCount: 2
    bounceElasticity: 1.8
    decelEasingSpeed: 0.16
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
