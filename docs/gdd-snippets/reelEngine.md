# reelEngine — GDD snippet

> Slot GDD Factory · reelEngine BLOCK (hot-path) The complete reel spin engine — column builder + tick loop + spin orchestrator + static-reroll fallback for non-uniform grids. This is the hottest path in the runtime; emitter bakes every magic number as a literal so the browser bundle never pays config-object dereference cost.

```yaml
reelEngine:
    minRotations: 5
    settleBreathMs: 60
    stripBufferCells: 2
    staticPreRollMs: 220
    staticBlurSwapMs: 220
    staticStaggerMs: 200
    staticHoldMs: 400
    staticSettleMs: 80
    staticFallbackMs: 60
    snapThreshold: 0.6
    minStepPx: 0.5
    accelMinFactor: 0.3
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
