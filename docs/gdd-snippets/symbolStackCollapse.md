# symbolStackCollapse — GDD snippet

> Wave W47.S18 — B75 · symbolStackCollapse block. Full-column "stack drop" celebration that fires when a tumble step clears an entire reel of the same symbol (or any clear that meets a configurable threshold).

```yaml
symbolStackCollapse:
    enabled: false
    triggerMode: same_symbol
    minStackHeight: 3
    minClearCount: 6
    durationMs: 1200
    autoCloseMs: 400
    flashColor: "255,235,150"
    accentColor: "255,200,80"
    showLabel: true
    labelTemplate: "+{N} {S}"
    haptic: false
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
