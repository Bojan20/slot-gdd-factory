# hookBus — GDD snippet

> Slot GDD Factory · hookBus BLOCK THE central lifecycle bus. Every feature block registers its runtime callbacks here; the spin engine (runOneBaseSpin / handlePostSpin / FSM_runNextFsSpin / runTumbleChain) emits events into it.

```yaml
hookBus:
    debugLog: false
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
