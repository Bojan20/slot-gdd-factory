# infinityReels — GDD snippet

> Wave H18 — Infinity Reels (grid expands per cascade-win counter presenter). Industry baseline (vendor-neutral): A modern industry pattern adds one extra reel to the visible strip every time a cascade-win lands on the previous spin (or step).

```yaml
infinityReels:
    enabled: false
    startCount: 5
    capCount: 12
    milestones: [6,8,10,12]
    position: top-right
    badgeBg: "rgba(0,0,0,0.65)"
    badgeColor: "#5dd1ff"
    activeBg: "#5dd1ff"
    activeColor: "#02121a"
    labelTemplate: "REELS {N}"
    fontSizePx: 13
    pulseMs: 420
    zIndex: 38
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
