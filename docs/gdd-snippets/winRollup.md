# winRollup — GDD snippet

> Slot GDD Factory · winRollup BLOCK Base-game total-win counter that ticks "TOTAL WIN: €X.XX" with a slot-machine digit-by-digit rollup whenever a regular win lands and stays as a steady display until the next spin. Sits ABOVE the hub (between the reels and the balance HUD) — Boki rule 05.06.2026: *"obican counter u base game iznad Hub-a koji stoji"*.

```yaml
winRollup:
    enabled: true
    labelText: TOTAL WIN
    bigWinTriggerRatio: 10
    minDurationMs: 400
    maxDurationMs: 2000
    msPerBetMultiple: 60
    holdMs: 0
    currency: €
    currencyPosition: prefix
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
