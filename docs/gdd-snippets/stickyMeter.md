# stickyMeter — GDD snippet

> Wave B70 — Sticky Symbol Counter HUD block. Industry baseline: many FS rounds expose a small counter that tracks how many sticky symbols (typically wilds) are currently anchored on the grid — e.g. "STICKY: 3 / 12".

```yaml
stickyMeter:
    enabled: false
    maxCap: 30
    showMaxCap: true
    position: bottom-left
    labelTemplate: "STICKY {N}"
    labelTemplateWithMax: "STICKY {N}/{M}"
    fontSizePx: 12
    bgColor: "rgba(0,0,0,0.55)"
    fgColor: "#f2f2f2"
    accentColor: "#c9a227"
    flashMs: 220
    zIndex: 34
    stickySelector: ".cell--sticky, .cell--locked, [data-sticky=\"true\"], [data-locked=\"true\"]"
    showInBase: false
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
