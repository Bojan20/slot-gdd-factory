# syncReels — GDD snippet

> Wave H19 — Sync Reels (2+ reels show identical symbol stack). Industry baseline (vendor-neutral): Feature in which a configured pair (or trio) of reels are forced to land with identical visible symbols, dramatically boosting line/way wins.

```yaml
syncReels:
    enabled: false
    flashColor: "rgba(255,216,77,0.65)"
    pulseMs: 540
    zIndex: 18
    autoDetect: true
    detectMinReels: 2
    persistRound: false
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
