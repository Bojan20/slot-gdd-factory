# insuranceBet — GDD snippet

> Wave LEGO-SIDEBET (B-7 · 1/2) — Insurance side bet. @module insuranceBet Purpose: Pre-spin toggle that lets the player add a small "insurance" wager (default +20% of bet). If the next spin returns 0 win, the insurance pays back N% of the total bet (default 50% of insurance stake).

```yaml
insuranceBet:
    enabled: false
    label: INSURE
    costMult: 1.2
    payoutRatio: 0.5
    color: "#7fbfff"
    colorDark: "#205080"
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
