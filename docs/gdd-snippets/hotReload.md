# hotReload — GDD snippet

> Wave P8 — **Hot-Reload bez page refresh** (dev-mode feedback loop). Industry pattern (template-neutral, vendor-neutral): Modern slot-game dev tooling exposes a `dev` server that watches the GDD source, the parser, and the block layer.

```yaml
hotReload:
    enabled: false
    endpoint: /__dev/events
    reconnectMs: 1500
    reconnectMaxMs: 10000
    debounceMs: 120
    fullReloadCategories: ["block","orchestrator","runtime"]
    fastReloadCategories: ["gdd","sample"]
    keepalivePingMs: 25000
    indicator: true
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
