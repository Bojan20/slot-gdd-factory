# winMultiplierBadge — GDD snippet

> Wave H20 — Win Multiplier Badge (per-line / per-win × N chip). Industry baseline (vendor-neutral): When a win line carries a multiplier (random per-line mult, path-aware mult, persistent mult), modern slots render a small "× N" badge on or near the affected payline so the player understands the source of the bonus.

```yaml
winMultiplierBadge:
    enabled: false
    minMult: 2
    fontSizePx: 13
    pulseMs: 340
    zIndex: 25
    position: win-center
    bgColor: "#ffd84d"
    fgColor: "#03110a"
    labelTemplate: "×{N}"
    clearOnNextSpin: true
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
