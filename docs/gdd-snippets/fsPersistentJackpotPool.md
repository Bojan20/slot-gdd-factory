# fsPersistentJackpotPool — GDD snippet

> Wave LEGO-FS2.2 — Persistent jackpot pool across FS spins. Purpose ─────── FS round maintains a JACKPOT POOL (×bet units) that: • starts at a configured floor on FS_TRIGGER, • increments by a fixed delta + optional fraction of EACH FS spin win, • carries over BETWEEN FS spins (does NOT reset per spin), • PAYS OUT in full on a configurable trigger condition: …

```yaml
fsPersistentJackpotPool:
    enabled: false
    floorX: 10
    deltaPerSpinX: 2
    winFraction: 0.1
    payoutTrigger: fsEnd
    chipColor: "#ffd84d"
    chipPosition: topLeft
    showChip: true
    maxScattersThreshold: 5
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
