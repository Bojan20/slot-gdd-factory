# cellLevelUpgrade — GDD snippet

> Wave H7 — Cell Level Upgrade (per-cell numeric level meter + badge). Industry baseline (vendor-neutral): Distinct from `symbolUpgrade.mjs` (which TRANSMUTES a low-tier symbol to a high-tier symbol on cascade refill), this block adds a *persistent numeric level meter* to individual grid cells.

```yaml
cellLevelUpgrade:
    enabled: false
    maxLevel: 9
    badgeFontSize: 10
    badgeColor: "#03110a"
    badgeBg: "#ffd84d"
    pulseMs: 320
    bumpOn: winCell
    scope: round
    resetOnFsTrigger: false
    resetOnFsEnd: true
    restrictToSymbols: []
    zIndex: 16
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
