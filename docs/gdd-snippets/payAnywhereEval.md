# payAnywhereEval — GDD snippet

> Wave K1 — Pay-anywhere (scatter pays) win evaluator. Replaces the line-based `detectLineWins()` when GDD declares `evaluation: 'pay_anywhere'` (pay-anywhere cluster reference).

```yaml
payAnywhereEval:
    enabled: false
    minWin: 8
    bucketEdges: [10,12]
    paytable: {}
    maxEvents: 9
    noWinChance: 0
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
