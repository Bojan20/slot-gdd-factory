# lockedSymbolFs — GDD snippet

> Wave LEGO-FSV.2 — Free Spins start with N symbols already locked. Purpose ─────── On FS_INTRO (before the first FS spin actually rolls), this block plants N "locked" symbol cells on the grid.

```yaml
lockedSymbolFs:
    enabled: false
    lockSymbol: W
    lockCount: 3
    lockColor: "#ffd84d"
    restrictToInnerReels: false
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
