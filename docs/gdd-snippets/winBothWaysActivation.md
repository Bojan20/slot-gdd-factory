# winBothWaysActivation — GDD snippet

> Wave LEGO-FS3.1 — FS-only "win both ways" activation. Purpose ─────── During FS rounds, the paywin direction expands from default LTR (left-to-right) to BOTH ways (LTR + RTL).

```yaml
winBothWaysActivation:
    enabled: false
    chipColor: "#7af2c8"
    chipPosition: top
    showChip: true
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
