# retriggerMultiplierBump — GDD snippet

> Wave LEGO-M4 — FS retrigger bumps the round multiplier by a fixed step or via a configured ladder. Purpose ─────── Every FS retrigger (additional scatters during FS) BUMPS the round- level multiplier by a fixed `step` (default +1) OR by the next value in a configured ladder (e.g. 1 → 2 → 3 → 5 → 10).

```yaml
retriggerMultiplierBump:
    enabled: false
    mode: step
    step: 1
    ladder: [1,2,3,5,10]
    chipColor: "#ffd84d"
    hudPosition: topRight
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
