# energyMeter — GDD snippet

> Wave B73 — Energy Meter side-feature gauge block. Industry baseline: many modern slot themes expose a metered side feature (e.g. "ENERGY 7/10") that fills on qualifying events (every spin, every win, every cascade step) and triggers a bonus when full.

```yaml
energyMeter:
    enabled: false
    capacity: 10
    perStep: 1
    fillOn: win
    scatterSymbol: S
    autoResetOnFull: true
    resetOnFsEnd: true
    position: top-left
    labelTemplate: "ENERGY {N}/{M}"
    fontSizePx: 12
    bgColor: "rgba(0,0,0,0.55)"
    fgColor: "#f2f2f2"
    fillColor: "#c9a227"
    fullColor: "#48d597"
    flashMs: 300
    zIndex: 35
    width: 120
    showInBase: true
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
