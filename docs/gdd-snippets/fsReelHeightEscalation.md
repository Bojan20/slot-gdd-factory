# fsReelHeightEscalation — GDD snippet

> Wave LEGO-FS3.2 — FS retrigger → reel height escalation. Purpose ─────── Each FS retrigger BUMPS the visible row count of every reel by `rowsPerRetrigger` (default +1), capped at `maxRows`.

```yaml
fsReelHeightEscalation:
    enabled: false
    rowsPerRetrigger: 1
    maxRows: 8
    fillSymbol: ?
    chipColor: "#ffd84d"
    showChip: true
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
