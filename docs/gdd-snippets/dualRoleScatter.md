# dualRoleScatter — GDD snippet

> Wave H10 — Dual-Role Scatter (scatter that doubles as wild or pay). Industry baseline (vendor-neutral): Many modern games extend the classic scatter so the symbol carries TWO semantic roles in different contexts: • Primary role : SCATTER — triggers FS / bonus when N+ land on the grid • Secondary : WILD — substitutes for any tier (HP/MP/LP) inside a winning c…

```yaml
dualRoleScatter:
    enabled: false
    symbol: S
    secondaryRole: wild
    minLanded: 1
    badgeText: ★
    badgeColor: "#03110a"
    badgeBg: "#ffd84d"
    pulseMs: 420
    zIndex: 17
    emitScatterRoleOnFsTrigger: true
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
