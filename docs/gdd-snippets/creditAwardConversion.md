# creditAwardConversion — GDD snippet

> Wave D-17.8 (industry-reference lock_respin family gap closure) — Single-source-of-truth credit → money conversion contract. Centralizes the canonical `coin_value = total_bet / fixedCoinCount` derivation + per-pay-type award-unit modes so every block in the repo reads the same award unit semantics.

```yaml
creditAwardConversion:
    enabled: false
    fixedCoinCount: 20
    payTypeModes:
      line: xCoin
      scatter: xTotalBet
      pattern: xTotalBet
      pot: credits
      holdAndWin: credits
      feature: credits
      wheel: xTotalBet
    defaultBet: 1
    emitOnBoot: true
    bodyAttrName: data-award-unit
    bodyAttrValue: credits
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
