# symbolInfoPopover — GDD snippet

> Slot GDD Factory · symbolInfoPopover BLOCK Wave V7 — tap / hover a grid cell → small popover with that symbol's tier + label + (placeholder) payout hint. Closes on outside tap, Escape, or new spin.

```yaml
symbolInfoPopover:
    enabled: true
    autoHideMs: 2400
    accentColor: "255,214,110"
    bgColor: "10,12,18"
    textColor: "245,242,228"
    showTierBadge: true
    showPayoutHint: true
    payoutHints: {}
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
