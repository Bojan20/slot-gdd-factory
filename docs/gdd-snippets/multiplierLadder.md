# multiplierLadder — GDD snippet

> Wave B67 — Persistent Multiplier Ladder UI block. Industry baseline: many FS rounds expose a discrete climbing-mult ladder (e.g. 1× → 2× → 3× → 5× → 10×).

```yaml
multiplierLadder:
    enabled: false
    steps: [1,2,3,5,10]
    startTier: 0
    position: top-right
    labelTemplate: "×{N}"
    fontSizePx: 14
    bgColor: "rgba(0,0,0,0.55)"
    fgColor: "#f2f2f2"
    accentColor: "#c9a227"
    flashMs: 280
    zIndex: 33
    climbOnBaseTumble: false
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
