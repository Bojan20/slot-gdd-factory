# totalMultiplierChip — GDD snippet

> Wave LEGO-M6 — Global accumulated-multiplier HUD chip. Purpose ─────── Universal HUD widget showing the CURRENT global multiplier — the product of every mult source active right now (orb sum, persistent, FS retrigger bump, wild collision product, cluster size tier, etc.).

```yaml
totalMultiplierChip:
    enabled: false
    position: topLeft
    fontSizePx: 16
    chipColor: "#ffe680"
    hideWhenOne: true
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
