# coinCollect — GDD snippet

> Wave LEGO-COLLECT (B-4 · 1/3) — Coin / Token collection mechanic. @module coinCollect Purpose: When GDD declares `coin_collect` (or `token_collect`), this block scans every `onSpinResult` for cells carrying a "coin" attribute (`data-coin-value` or a configured symbol id), tallies their value into a running session total exposed on `window.__COIN_COLLECT__`, …

```yaml
coinCollect:
    enabled: false
    coinSymbolId: COIN
    defaultCoinValue: 1
    pauseDuringFs: false
    resetOnFsEnd: false
    color: "#ffd34a"
    haloRGB: "255,211,74"
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
