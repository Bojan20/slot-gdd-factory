# backendSpinEngine — GDD snippet

> LV3-3 — Backend spin engine adapter (MATH-INTEGRATION-LV3 · Boki 2026-06-24). Purpose Wraps the in-browser spin pipeline tako da svaki postSpin tick takođe poziva math-backend HTTP server `POST /spin` da povuče per-spin outcome (payX) iz cached Rust batch distribucije.

```yaml
backendSpinEngine:
    enabled: true
    backendBase: "http://127.0.0.1:9001"
    probeTimeoutMs: 1500
    spinTimeoutMs: 3000
    maxConsecutiveErrors: 3
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
