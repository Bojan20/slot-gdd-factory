# hexReelEngine — GDD snippet

> Wave J2b — Hex real reel engine. Industry-reference rationale ──────────────────────────── Honeycomb / hex slot layouts (axial q,r coordinates) cannot reuse the rectangular reel engine because each cell carries an `(r % 2)` x-shift that breaks "straight column" assumptions.

```yaml
hexReelEngine:
    enabled: true
    spinDurationMs: 1800
    staggerPerColumnMs: 280
    minRotations: 8
    cushionBouncePx: 6
    cushionBounceMs: 240
    fadeFallbackMs: 200
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
