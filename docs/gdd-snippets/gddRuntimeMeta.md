# gddRuntimeMeta — GDD snippet

> Wave RENDER-INTEG-A (2026-06-23) — GDD runtime meta-emit block. @module gddRuntimeMeta Purpose: Surfaces parser-extracted GDD math/compliance fields to the runtime environment as `window.__GDD_*__` constants so other blocks, tests, QA tooling, and operator HUD overlays can consume them without re-reading model.json. This closes the render-integration gap whe…

```yaml
gddRuntimeMeta:
    enabled: true
    emitCompliance: true
    emitRtpBreakdown: true
    emitScatterPayTable: true
    emitExpandingWildOnlyIfWinning: true
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
