# nudgeReel — GDD snippet

> Wave H17 — Nudge Reel (classic fruit-machine near-miss rescue presenter). Industry baseline (vendor-neutral): On near-miss spins (e.g. scatter 2 visible + 1 just above the viewport), classic fruit-machine cabinets offered the player a one-shot "NUDGE" button that nudged a specific reel one tile up or down to convert the near-miss into a real trigger.

```yaml
nudgeReel:
    enabled: false
    offerMs: 6000
    position: bottom-left
    chipLabel: NUDGE
    chipBg: "#5dd1ff"
    chipColor: "#02121a"
    allowDuringFs: false
    autoDeclineOnSpin: true
    zIndex: 45
    animMs: 420
    fontSizePx: 13
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
