# cumulativeMeter — GDD snippet

> Wave LEGO-COLLECT (B-4 · 2/3) — Session-cumulative HUD meter. @module cumulativeMeter Purpose: Visible HUD widget (top-right corner default) that mirrors the `window.__COIN_COLLECT__.sessionTotal` running total from `coinCollect.mjs`. Renders a horizontal progress bar from current value toward the next configured threshold, plus a numeric label.

```yaml
cumulativeMeter:
    enabled: false
    label: COINS
    thresholds:
      - 
              id: bronze
              value: 50
              awardKind: credit
              awardValue: 25
      - 
              id: silver
              value: 200
              awardKind: credit
              awardValue: 150
      - 
              id: gold
              value: 1000
              awardKind: fs_trigger
              awardValue: 10
    resetMode: subtract
    color: "#ffd34a"
    colorDark: "#a07520"
    haloRGB: "255,211,74"
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
