# wheelBonusReveal — GDD snippet

> Wave W47.S18 — B72 · wheelBonusReveal block. Extension presenter that sits on top of the existing `wheelBonus.mjs` (mini-game) and `weightedWheelSegments.mjs` (weighted resolution layer).

```yaml
wheelBonusReveal:
    enabled: false
    revealStyle: zoom
    highlightDurationMs: 1200
    autoCloseMs: 800
    haloColor: "255,210,90"
    centerMessage: "YOU WON {label}"
    jackpotMinValue: 100
    jackpotMessage: "JACKPOT! {label}"
    jackpotHaloColor: "255,80,80"
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
