# spinHistoryReplay — GDD snippet

> Wave LEGO-REPLAY (B-2 · 1/2) — Spin history buffer + replay engine. @module spinHistoryReplay Purpose: Captures the last N spin outcomes (grid snapshot + win + timestamp) into a ring buffer on `window.__SPIN_HISTORY__`. Exposes a replay API consumed by `replayControlBar.mjs` (the player-facing UI).

```yaml
spinHistoryReplay:
    enabled: false
    label: REPLAY
    maxBuffer: 10
    captureFs: false
    color: "#7fffd4"
    haloRGB: "127,255,212"
```

_Generated by `tools/gen-gdd-snippets.mjs`. Paste under the parser-
recognised GDD section to override defaults per game._
